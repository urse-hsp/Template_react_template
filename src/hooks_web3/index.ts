import {
  useWeb3Provider,
  useEthScanPath,
  useSingleResult,
  useHashMessage,
  getContract,
} from '@infte/web3modal-react';

export { useEthScanPath, useSingleResult, useHashMessage, getContract };
export { useWeb3Provider };

export const useContract = () => {};
export const useBalance = () => {};
